package com.ixoquiz.quiz.dialect;

import org.hibernate.dialect.Dialect;

public class SQLiteDialect extends Dialect{
	
	@SuppressWarnings("deprecation")
	public SQLiteDialect() {
        super();
    }
}
